[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kdenlive-face-tracking-tool"
version = "0.1.0"
description = "Offline Kdenlive face-tracking mask generator"
readme = "README.md"
requires-python = ">=3.12"
keywords = ["kdenlive", "face-tracking", "video", "opencv", "onnxruntime"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Environment :: Console",
    "Topic :: Multimedia :: Video",
]
dependencies = [
    "numpy>=2.2.2",
    "opencv-python>=4.10.0.84",
    "insightface>=0.7.3",
    "onnxruntime>=1.20.1",
]

[project.scripts]
kdenlive-face-mask = "kdenlive_face_mask:main"

[project.optional-dependencies]
cuda = [
    "onnxruntime-gpu>=1.20.1",
]
rocm = [
    "onnxruntime-rocm>=1.20.1",
]

[tool.setuptools]
py-modules = ["kdenlive_face_mask"]

[tool.uv]
package = true
